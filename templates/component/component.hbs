import { Translate } from '~/i18n';
import { useAuth } from '~/auth';

/******************************************************************
 *
 *  THIS COMPONENT WAS GENERATED BY NEXTCRAZY-CLI
 *
 ******************************************************************/

const {{name}} = () => {
   const { authenticated } = useAuth();

   return (
      <div>
         <Translate id="common:some-key" />

         {authenticated && <div>am protected</div>}
      </div>
   );
};

export default {{name}};
